import React from 'react';
import ReactDOM from 'react-dom/client';
import { Aplication } from './styles/stylesApp';
import '../src/index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <Aplication />
  </React.StrictMode>,
);
